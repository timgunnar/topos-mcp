{
  "name": "topos",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev -w @topos/mcp & npm run dev -w @topos/dashboard"
  }
}
